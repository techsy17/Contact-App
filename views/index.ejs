<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contacts Home</title>
    <style>* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

/* Body */
body {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    background: linear-gradient(to right, #6a11cb, #2575fc);
    color: #333;
}

/* Navbar */
nav {
    width: 100%;
    background: rgba(0,0,0,0.7);
    padding: 15px 50px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: white;
    position: sticky;
    top: 0;
    z-index: 100;
}

nav h1 {
    font-size: 24px;
    font-weight: bold;
}

nav a {
    color: white;
    text-decoration: none;
    margin-left: 20px;
    padding: 8px 15px;
    border-radius: 5px;
    transition: 0.3s;
}

nav a:hover {
    background: #ffc107;
    color: #000;
}

/* Header */
header {
    text-align: center;
    padding: 60px 20px;
    color: white;
}

header h2 {
    font-size: 48px;
    margin-bottom: 10px;
    text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
}

header p {
    font-size: 20px;
    color: #f0f0f0;
}

/* Contact List */
ul {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
    padding: 20px;
    list-style: none;
}

/* Contact Card */
li {
    background: #fff;
    padding: 20px;
    width: 300px;
    border-radius: 15px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.2);
    display: flex;
    flex-direction: column;
}

li h3 {
    color: #2575fc;
    margin-bottom: 10px;
    font-size: 22px;
}

li p {
    margin-bottom: 8px;
}

/* Buttons container */
.card-buttons {
    display: flex;
    gap: 10px;
    margin-top: 12px;
}

/* Common button style */
.card-buttons a,
.card-buttons button {
    padding: 6px 14px;
    font-size: 14px;
    border-radius: 5px;
    font-weight: 600;
    cursor: pointer;
}

/* Edit button */
.card-buttons a {
    background: #28a745;
    color: white;
    text-decoration: none;
}

.card-buttons a:hover {
    background: #218838;
}

/* Delete button */
.card-buttons button {
    background: #dc3545;
    color: white;
    border: none;
}

.card-buttons button:hover {
    background: #c82333;
}

/* IMPORTANT: delete form reset */
.delete-form {
    padding: 0;
    margin: 0;
    background: none;
    box-shadow: none;
}

/* Add Contact Button */
.add-contact {
    display: inline-block;
    background: #ffc107;
    color: #000;
    padding: 12px 24px;
    margin: 20px auto;
    border-radius: 6px;
    text-decoration: none;
    transition: 0.3s;
}

.add-contact:hover {
    background: #e0a800;
}

/* Main Forms (Add / Edit pages only) */
form:not(.delete-form) {
    background: rgba(255, 255, 255, 0.95);
    padding: 40px 30px;
    max-width: 500px;
    margin: 40px auto;
    border-radius: 15px;
    box-shadow: 0 15px 30px rgba(0,0,0,0.2);
}

form:not(.delete-form) h2 {
    text-align: center;
    color: #2575fc;
    margin-bottom: 30px;
    font-size: 32px;
}

form:not(.delete-form) label {
    display: block;
    margin-bottom: 5px;
    font-weight: 600;
}

form:not(.delete-form) input {
    width: 100%;
    padding: 12px 15px;
    margin-bottom: 20px;
    border: 1px solid #ccc;
    border-radius: 8px;
    font-size: 16px;
}

form:not(.delete-form) input:focus {
    border-color: #2575fc;
    box-shadow: 0 0 8px rgba(37, 117, 252, 0.4);
    outline: none;
}

form:not(.delete-form) button {
    width: 100%;
    padding: 14px;
    background: #2575fc;
    color: white;
    font-size: 18px;
    font-weight: 600;
    border: none;
    border-radius: 10px;
    cursor: pointer;
}

form:not(.delete-form) button:hover {
    background: #1a5dcc;
}

/* Footer */
footer {
    margin-top: auto;
    text-align: center;
    padding: 20px;
    background: rgba(0,0,0,0.7);
    color: white;
}</style>
    <!-- <link rel="stylesheet" href="/styles.css"> -->
</head>
<body>

<%- include('partials/header', { pageTitle: 'All Contacts', pageSubtitle: 'Manage your contacts easily' }) %>

<ul>
    <% contacts.forEach(contact => { %>
        <li>
            <h3><%= contact.name %></h3>
            <p>📞 <%= contact.phone %></p>
            <p>✉️ <%= contact.email %></p>

            <div class="card-buttons">
                <a href="/api/edit/<%= contact._id %>">Edit</a>

                <!-- IMPORTANT CLASS -->
                <form action="/api/delete/<%= contact._id %>?_method=DELETE"
                      method="post"
                      class="delete-form">
                    <button type="submit">Delete</button>
                </form>
            </div>
        </li>
    <% }) %>
</ul>

<%- include('partials/footer') %>

</body>
</html>